
function Circle(radius) {
    this.radius = radius

    this.defaultLocation = {x: 0, y: 0}

    this.computeOptimumLocation = function() {
        // do some fancy stuff
    }

    this.draw = function() {
        console.log('drwaing..')
    }
}